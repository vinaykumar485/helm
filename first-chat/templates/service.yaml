apiVersion: v1
kind: Service
metadata:
  name: {{ include "first-chat.fullname" . }}
  labels:
    {{- include "first-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "first-chat.selectorLabels" . | nindent 4 }}
